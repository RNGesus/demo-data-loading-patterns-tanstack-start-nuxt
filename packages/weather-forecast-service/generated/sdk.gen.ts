// This file is auto-generated by @hey-api/openapi-ts

import type { Client, Options as Options2, TDataShape } from './client';
import { client } from './client.gen';
import type { GetTwentyFourHrForecastData, GetTwentyFourHrForecastErrors, GetTwentyFourHrForecastResponses } from './types.gen';
import { zGetTwentyFourHrForecastData, zGetTwentyFourHrForecastResponse } from './zod.gen';

export type Options<TData extends TDataShape = TDataShape, ThrowOnError extends boolean = boolean> = Options2<TData, ThrowOnError> & {
    /**
     * You can provide a client instance returned by `createClient()` instead of
     * individual options. This might be also useful if you want to implement a
     * custom client.
     */
    client?: Client;
    /**
     * You can pass arbitrary values through the `meta` object. This can be
     * used to access values that aren't defined as part of the SDK function.
     */
    meta?: Record<string, unknown>;
};

/**
 * Retrieve the latest 24 hour weather forecast
 *
 * **[https://api-open.data.gov.sg/v2/real-time/api/twenty-four-hr-forecast](https://api-open.data.gov.sg/v2/real-time/api/twenty-four-hr-forecast)**
 *
 * <br/>
 *
 * - Updated multiple times throughout the day
 *
 * <br/>
 *
 * - Filter for specific date or date-time by providing `date` in query parameter.
 * - use YYYY-MM-DD format to retrieve all of the readings for that day
 * - use YYYY-MM-DDTHH:mm:ss to retrieve the latest readings at that moment in time
 * - example: `?date=2024-07-16` or `?date=2024-07-16T23:59:00`
 *
 * <br/>
 *
 * - Possible values for forecast include:
 * - Fair
 * - Fair (Day)
 * - Fair (Night)
 * - Fair and Warm
 * - Partly Cloudy
 * - Partly Cloudy (Day)
 * - Partly Cloudy (Night)
 * - Cloudy
 * - Hazy
 * - Slightly Hazy
 * - Windy
 * - Mist
 * - Fog
 * - Light Rain
 * - Moderate Rain
 * - Heavy Rain
 * - Passing Showers
 * - Light Showers
 * - Showers
 * - Heavy Showers
 * - Thundery Showers
 * - Heavy Thundery Showers
 * - Heavy Thundery Showers with Gusty Winds
 *
 */
export const getTwentyFourHrForecast = <ThrowOnError extends boolean = false>(options?: Options<GetTwentyFourHrForecastData, ThrowOnError>) => (options?.client ?? client).get<GetTwentyFourHrForecastResponses, GetTwentyFourHrForecastErrors, ThrowOnError, 'data'>({
    requestValidator: async (data) => await zGetTwentyFourHrForecastData.parseAsync(data),
    responseValidator: async (data) => await zGetTwentyFourHrForecastResponse.parseAsync(data),
    responseStyle: 'data',
    url: '/twenty-four-hr-forecast',
    ...options
});
